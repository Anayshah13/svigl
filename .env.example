# Backend
APP_NAME=API
DEBUG=false
BACKEND_PORT=8000

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=app
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_HOST_PORT=5433

# Google OAuth — set in backend/.env.local (see backend/.env.example)
